import React from 'react';
import styled from 'styled-components';
import { Grid, Text, Button } from '../elements';

const InfoWrite = (props) => {
  return (
    <React.Fragment>
      <Grid padding="10%" backgroundColor="#E5E5E5"></Grid>
    </React.Fragment>
  );
};

export default InfoWrite;
